apiVersion: v1
kind: Service
metadata:
  name: devops-platform
  namespace: devops-platform
spec:
  selector:
    app: devops-platform
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
